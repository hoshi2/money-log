# 開発の経緯メモ（money-log / 旧STELLA FINANCE）

2026年6月初旬にゼロから作ったReactアプリ。GitHub Pagesで公開している。

- 位置づけ：個人・法人の財務管理と、ローン返済計画の可視化
- 全データを画面上で編集でき、ブラウザの `localStorage`（端末内の保存場所）に自動保存
- 旧リポジトリ名は `-`（ハイフン1文字）。2026-09-20 に `money-log` へ改名
- セキュリティ確認済み：APIキーの漏洩リスクは検討済みで、`localStorage` を使う設計自体に
  問題はないと判断した
- このアプリを作るときに、Mac側のGit／Node.jsの初期設定もコマンドからゼロで行った

出典：`rules-docs/docs/webapp.md` を2026-09-20にアプリごとに分割したもの。
